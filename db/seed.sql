-- Este arquivo roda automaticamente na primeira inicialização do banco.
-- Dados de exemplo para teste — remova em produção.

-- A tabela é criada pelo backend na inicialização (src/db/init.js).
-- O seed.sql existe apenas para dados de demonstração.

-- Nota: aguarda o backend criar as tabelas antes de inserir.
-- Por isso deixamos o seed vazio aqui; o initDB() do Node cuida da estrutura.
SELECT 1; -- placeholder para evitar erro de arquivo vazio
